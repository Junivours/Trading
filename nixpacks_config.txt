[phases.setup]
nixPkgs = ['python311', 'gcc', 'gfortran', 'pkg-config']

[phases.install]
cmds = [
    'pip install --upgrade pip',
    'pip install wheel setuptools',
    'pip install numpy',
    'pip install TA-Lib',
    'pip install -r requirements.txt'
]

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT --workers 4 --timeout 120 --preload app:app'